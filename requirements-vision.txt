# requirements-vision.txt - optional vision dependencies.
#
# OpenCV captures camera frames, and MediaPipe Pose estimates 2D body landmarks.
#
# Install:
#   pip install -r requirements-vision.txt

# OpenCV: camera capture and image processing.
opencv-python>=4.10

# MediaPipe: realtime 2D pose landmark detection.
mediapipe>=0.10
